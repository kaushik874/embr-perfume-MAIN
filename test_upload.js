import 'dotenv/config'; import { db } from './server/db.ts'; import fs from 'fs'; async function main() { const data = fs.readFileSync('test_db_image.png').toString('base64'); const body = { name: 'test.png', data: 'data:image/png;base64,' + data }; const res = await fetch('http://localhost:3001/api/admin/hero/upload', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); console.log(res.status); console.log(await res.text()); process.exit(0); } main().catch(console.error);
